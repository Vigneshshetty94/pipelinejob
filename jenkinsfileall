pipeline {
agent any
// parameter {
// string (name: 'NAME'
 // triggers {
// cron('H/15 * * * *')
 // }
environment {
	NAME= "Vignesh"
}
stages {
 	stage('Build') {	
	    steps {
	    // echo "NAME ${params.NAME}"
		 echo "NAME= ${env.NAME}"
                 sh 'sleep 5'
			}
		}
	}
}
